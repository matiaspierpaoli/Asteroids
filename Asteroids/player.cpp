//#include "player.h"
//#include "header.h"
//
//Player::Player()
//{
//	speed = Vector2{ 0, 0 };
//	acceleration = 0;
//	rotation = 0;
//	color = RED;
//	size = 20.0f;
//	height = (size / 2) / tanf(20 * DEG2RAD);
//	position = Vector2{ screenWidth / 2, screenHeight / 2 - height / 2 };
//	collider = Vector3{ position.x + sin(rotation * DEG2RAD) * (height / 2.5f), position.y - cos(rotation * DEG2RAD) * (height / 2.5f), 12 };
//}
